# Implementación de imágenes: Prelaboratorio 3 — Circuito Magnético de un Contactor

Se han incorporado exitosamente las imágenes solicitadas en el archivo `maquinas-electricas-i/entregables/prelaboratorios/prelab-3/main.tex`:

1. **contactor.jpg**: Añadido en la subsección "Definición y Partes del Contactor" como una figura centrada, con el label `fig:contactor` y un ancho de `0.45\textwidth`.
2. **despiece_contactor.jpg**: Añadido en la misma subsección como una figura centrada con el despiece y partes constitutivas, con el label `fig:despiece_contactor` y un ancho de `0.7\textwidth`.

## Verificación
- Se compiló dos veces seguidas para resolver correctamente las referencias cruzadas mediante `pdflatex` con el PATH libre de `/mnt/c/` en WSL.
- Ejecución limpia de `./init.sh`.
